<%= form_for(webservice) do |f| %>
  <% if webservice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(webservice.errors.count, "error") %> prohibited this webservice from being saved:</h2>

      <ul>
      <% webservice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :wsdl %>
    <%= f.text_area :wsdl %>
  </div>

  <div class="field">
    <%= f.label :url %>
    <%= f.text_area :url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
